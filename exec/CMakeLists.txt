# add submodule directories
if (ENABLE_EXS)
   add_subdirectory(exs)
endif()
if (ENABLE_PUQ)
   add_subdirectory(puq)
endif()
if (ENABLE_DIP) 
   add_subdirectory(dip) 
endif()
if (ENABLE_EXS AND ENABLE_VAL AND ENABLE_PUQ AND ENABLE_DIP)
add_subdirectory(snt)
endif()
