# DIPL Skylight highlighter

`dipl.xml` is a Kate/Skylight language definition for DIPL files. Install or
copy it into the editor’s language-definition directory, then select **DIPL**
for files with the `.dip` or `.dipl` extension. The definition highlights
declarations, data types, directives, branching, references, numbers,
strings, units, and comments.
