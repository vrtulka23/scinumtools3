# add submodule directories
if (ENABLE_PUQ)
   add_subdirectory(puq)
endif()
if (ENABLE_DIP) 
   add_subdirectory(dip) 
endif()
if (ENABLE_EXS OR ENABLE_VAL OR ENABLE_PUQ OR ENABLE_DIP)
add_subdirectory(snt)
endif()
