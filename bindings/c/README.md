# C binding

The C binding provides opaque handles over the public SNT C++ classes. The
initial PUQ interface supports expression evaluation, unit conversion,
formatted output, explicit destruction, and C-compatible error reporting.
